clear
clc
%% Cases
    test = 'Poissson1D 2(f)1';
%   test = 'Poissson1D 2(f)2';
%   test = 'Poissson1D 2(f)3';

%% Initial Values
M = 4;
h = 1/M;
u = zeros(M-1,1);
e = ones(M-1,1);
A = spdiags([-e 2*e -e],-1:1,M-1,M-1);

%% Boundary
switch test
    case 'Poissson1D 2(f)1'
        